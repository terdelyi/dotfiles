# Work machine only. Installed after Brewfile when ~/.dotfiles.profile
# says "work".

# Communication
cask 'slack'

# Networking
# The cask, not `mas 'Tailscale'`: a work Apple Account is usually managed and
# cannot acquire App Store apps at all. Same application, same auto-updates.
cask 'tailscale-app'
